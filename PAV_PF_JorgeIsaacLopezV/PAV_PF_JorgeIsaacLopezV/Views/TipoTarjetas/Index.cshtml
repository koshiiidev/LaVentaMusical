@model IEnumerable<PAV_PF_JorgeIsaacLopezV.TipoTarjeta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_tarjeta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_tarjeta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_tarjeta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_tarjeta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_tarjeta })
        </td>
    </tr>
}

</table>
